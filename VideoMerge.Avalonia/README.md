# VideoMerge 桌面版（Avalonia）

瀏覽器版的桌面對應：本機 FFmpeg 合併、片段首尾幀、每段可單獨播放。

## 啟動

- 專案根目錄雙擊 `start-desktop.bat`
- 或 `dotnet run --project VideoMerge.Avalonia -c Release`

需要 .NET 8 SDK，以及系統上的 `ffmpeg` / `ffprobe`。
